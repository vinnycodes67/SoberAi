## Design System

Always read `DESIGN.md` before making visual or UI decisions. All typography, color, spacing, motion, and Liquid Glass rules are defined there. Do not deviate without explicit user approval. In QA mode, flag UI code that does not match `DESIGN.md`.
