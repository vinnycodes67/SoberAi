# Markdown uses two trailing spaces as a hard line break, so whitespace
# checking would flag intentional formatting as an error.
*.md -whitespace

# Binary assets: no diff, no whitespace checking.
*.otf binary
*.png binary
*.svg -whitespace
